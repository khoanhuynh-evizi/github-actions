name: Smoke Test Daily (-s)

on:
  schedule:
    - cron: '0 20 * * 0-4' # 03:00h every night UTC+7
  workflow_dispatch:
    inputs:
      suite:
        description: 'Suite name'
        required: true
        default: 'Smoke Test'
      features:
        description: 'Feature (Optional)'
        required: false
        default: ''
      featureTestCases:
        description: 'Specific testcase (Optional)'
        required: false
        default: ''
      branchWdio:
        description: 'Branch WDIO'
        required: true
        default: 'master'
      branchDashboard:
        description: 'Branch dashboard'
        required: false
        default: 'develop'
      dashboardUrl:
        description: 'Dashboard URL (Optional)'
        required: false
        default: ''
      forceRun:
        description: 'Force run (skip commit check)'
        required: false
        default: 'false'
        type: boolean

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: true

jobs:
  check-repos-commit:
    runs-on: ubuntu-latest
    outputs:
      should_run: ${{ steps.check.outputs.should_run }}

    steps:
      - name: Check latest commits in WDIO & Dashboard repos
        id: check
        run: |

          WDIO_REPO="${{ secrets.WDIO_REPOS }}"
          DASHBOARD_REPO="${{ secrets.DASHBOARD_REPO }}"

          WDIO_TOKEN="${{ secrets.WDIO_ACCESS_TOKEN }}"
          DASHBOARD_TOKEN="${{ secrets.DASHBOARD_ACCESS_TOKEN }}"

          NOW_TS=$(date +%s)
          SHOULD_RUN_FILE=$(mktemp)
          echo "false" > "$SHOULD_RUN_FILE"

          check_repo () {
            REPO=$1
            BRANCH=$2
            TOKEN=$3

            echo "----------------------------------------"
            echo "Checking repo: $REPO"
            echo "Branch: $BRANCH"

            COMMIT_JSON=$(curl -s \
              -H "Authorization: token $TOKEN" \
              https://api.github.com/repos/$REPO/commits/$BRANCH)

            COMMIT_DATE=$(echo "$COMMIT_JSON" | jq -r '.commit.committer.date')
            COMMIT_SHA=$(echo "$COMMIT_JSON" | jq -r '.sha')

            if [ "$COMMIT_DATE" = "null" ] || [ -z "$COMMIT_DATE" ]; then
              echo "❌ Failed to fetch commit from $REPO/$BRANCH — check token & repo name"
              exit 1
            fi

            echo "Latest commit SHA: $COMMIT_SHA"
            echo "Latest commit date (UTC): $COMMIT_DATE"
            echo "Latest commit local time (UTC+7): $(TZ=Asia/Ho_Chi_Minh date -d "$COMMIT_DATE" '+%Y-%m-%d %H:%M:%S')"

            COMMIT_TS=$(date -d "$COMMIT_DATE" +%s)
            DIFF=$((NOW_TS - COMMIT_TS))

            HOURS=$((DIFF / 3600))

            echo "Seconds since last commit: $DIFF"
            echo "Hours since last commit: $HOURS h"

            if [ $DIFF -le 86400 ]; then
              echo "Commit within 24h → TEST WILL RUN"
              echo "true" > "$SHOULD_RUN_FILE"
            else
              echo "No commit in last 24h"
            fi
          }

          if [ "${{ github.event.inputs.forceRun }}" = "true" ]; then
            echo "forceRun=true → skipping commit check, TEST WILL RUN"
            echo "true" > "$SHOULD_RUN_FILE"
          else
            check_repo "$WDIO_REPO" "${{ github.event.inputs.branchWdio || 'master' }}" "$WDIO_TOKEN"
            check_repo "$DASHBOARD_REPO" "${{ github.event.inputs.branchDashboard || 'develop' }}" "$DASHBOARD_TOKEN"
          fi

          SHOULD_RUN=$(cat "$SHOULD_RUN_FILE")
          rm -f "$SHOULD_RUN_FILE"

          echo "----------------------------------------"
          echo "Final decision should_run=$SHOULD_RUN"

          echo "should_run=$SHOULD_RUN" >> $GITHUB_OUTPUT

  chrome-ubuntu:
    needs: check-repos-commit
    if: needs.check-repos-commit.outputs.should_run == 'true'
    runs-on: ubuntu-latest
    env:
      TZ: Asia/Ho_Chi_Minh

    steps:
    - name: Checkout workflow repo
      uses: actions/checkout@v4

    - name: Checkout WDIO repository
      uses: actions/checkout@v4
      with:
        repository: ${{ secrets.WDIO_REPOS }}
        token: ${{ secrets.WDIO_ACCESS_TOKEN }}
        ref: ${{ github.event.inputs.branchWdio || 'master' }}
        path: wdio

    - name: Free disk space
      run: |
        df -h
        sudo rm -rf /usr/share/dotnet
        sudo rm -rf /usr/local/lib/android
        sudo rm -rf /opt/ghc
        sudo rm -rf "$AGENT_TOOLSDIRECTORY"
        sudo apt-get clean
        df -h    

    - name: Use Node.js 20.x
      uses: actions/setup-node@v4
      with:
        node-version: 20.x
        cache: yarn
        cache-dependency-path: wdio/yarn.lock

    - name: Install dependencies
      run: yarn install --frozen-lockfile
      working-directory: wdio

    - name: Clean old reports
      run: rm -rf wdio/reports wdio/allure-report wdio/allure-history specs-reporter.log

    - name: Run WebDriverIO Tests
      id: wdio
      run: |
        cd wdio
        yarn test:local \
            --suite="${{ github.event.inputs.suite || 'Smoke Test'}}" \
            --features="${{ github.event.inputs.features }}" \
            --featureTestCases="${{ github.event.inputs.featureTestCases }}" \
          2>&1 | tee ../specs-reporter.log
      env:
        CAPABILITIES: chrome
        MAX_INSTANCES: 2
        R360_DASHBOARD_URL: ${{ github.event.inputs.dashboardUrl || secrets.R360_DASHBOARD_URL_S }}

    - name: WDIO Test Checker
      if: always()
      run: |
        if grep -qE "[0-9]+\s+failed" specs-reporter.log; then
          exit 1
        fi

    - name: Upload spec reporter log
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: specs-reporter-log
        path: specs-reporter.log
        retention-days: 7

    - name: Get Allure history
      if: ${{ !cancelled() }}
      uses: actions/checkout@v4
      continue-on-error: true
      with:
        repository: ${{ secrets.WDIO_REPOS }}
        token: ${{ secrets.WDIO_ACCESS_TOKEN }}
        ref: allure-history
        path: report-history

    - name: Copy previous Allure history to reports (for trend graphs)
      if: ${{ !cancelled() }}
      run: |
        mkdir -p wdio/reports/history
        cp -r report-history/allure-history/last-history/. wdio/reports/history 2>/dev/null || \
        cp -r report-history/last-history/. wdio/reports/history 2>/dev/null || true

    - name: Generate Allure Report
      if: ${{ !cancelled() }}
      run: npx --yes allure-commandline@2.37.0 generate wdio/reports --clean -o wdio/allure-report

    - name: Save history for next run
      if: ${{ !cancelled() }}
      run: |
        mkdir -p allure-history-branch/allure-history
        cp -r wdio/allure-report/history allure-history-branch/allure-history/last-history

    - name: Push report history to branch
      if: ${{ !cancelled() }}
      uses: peaceiris/actions-gh-pages@v4
      with:
        personal_token: ${{ secrets.WDIO_ACCESS_TOKEN }}
        external_repository: ${{ secrets.WDIO_REPOS }}
        publish_branch: allure-history
        publish_dir: allure-history-branch

    - name: Prepare deploy folder
      if: ${{ !cancelled() }}
      run: |
        mkdir -p deploy
        cp -r wdio/allure-report/* deploy/

    - name: Upload Allure HTML
      if: ${{ !cancelled() }}
      uses: actions/upload-artifact@v4
      with:
        name: reports-html
        path: wdio/allure-report/
        retention-days: 7

    - name: Upload Allure raw results
      if: ${{ !cancelled() }}
      uses: actions/upload-artifact@v4
      with:
        name: reports-allure
        path: wdio/reports/
        retention-days: 7

    - name: Create firebase config
      if: ${{ !cancelled() }}
      run: |
        echo '{
          "hosting": {
            "public": "deploy",
            "ignore": ["firebase.json", "**/.*", "**/node_modules/**"],
            "limit": 3
          }
        }' > firebase.json

        echo '{
          "projects": {
            "default": "record360-report-s"
          }
        }' > .firebaserc

    - name: Set up Firebase credentials
      if: ${{ !cancelled() }}
      run: echo '${{ secrets.L_FIREBASE_SERVICE_ACCOUNT }}' > $GITHUB_WORKSPACE/service-account.json

    - name: Deploy Allure Report to Firebase Hosting
      if: ${{ !cancelled() }}
      uses: w9jds/firebase-action@v13.27.0
      with:
        args: deploy --only hosting --project record360-report-s
      env:
        GOOGLE_APPLICATION_CREDENTIALS: ${{ github.workspace }}/service-account.json

    - name: Parse Spec Reporter Result
      if: ${{ !cancelled() }}
      run: |
        LOG=specs-reporter.log
        if ! grep -q "Spec Files:" "$LOG"; then
          echo "TOTAL=0" >> $GITHUB_ENV
          echo "PASSED=0" >> $GITHUB_ENV
          echo "FAILED=0" >> $GITHUB_ENV
          echo "SKIPPED=0" >> $GITHUB_ENV
          echo "RETRIES=0" >> $GITHUB_ENV
          echo "DURATION=00:00:00" >> $GITHUB_ENV
          exit 0
        fi

        LINE=$(grep "Spec Files:" "$LOG" | tail -1)

        PASSED=$(echo "$LINE" | grep -oP '\d+(?=\s+passed)' || echo 0)
        RETRIES=$(echo "$LINE" | grep -oP '\d+(?=\s+retries)' || echo 0)
        FAILED=$(echo "$LINE" | grep -oP '\d+(?=\s+failed)' || echo 0)
        SKIPPED=$(echo "$LINE" | grep -oP '\d+(?=\s+skipped)' || echo 0)
        TOTAL=$(echo "$LINE" | grep -oP '\d+(?=\s+total)' || echo 0)
        DURATION=$(echo "$LINE" | grep -oP '\d{2}:\d{2}:\d{2}' || echo "00:00:00")

        PASS_RATE=$(( PASSED * 100 / (TOTAL - SKIPPED)))

        echo "TOTAL=$TOTAL" >> $GITHUB_ENV
        echo "PASSED=$PASSED" >> $GITHUB_ENV
        echo "FAILED=$FAILED" >> $GITHUB_ENV
        echo "SKIPPED=$SKIPPED" >> $GITHUB_ENV
        echo "RETRIES=$RETRIES" >> $GITHUB_ENV
        echo "PASS_RATE=$PASS_RATE" >> $GITHUB_ENV
        echo "DURATION=$DURATION" >> $GITHUB_ENV

    - name: Debug - Print raw failed lines from log
      if: ${{ !cancelled() }}
      run: |
        echo "=== Lines containing 'fail' (case-insensitive) ==="
        grep -in "fail" specs-reporter.log || echo "(none found)"
        
        echo ""
        echo "=== Lines containing 'passing' or 'pending' ==="
        grep -in "passing\|pending\|failing" specs-reporter.log || echo "(none found)"
        
        echo ""
        echo "=== Full log (last 100 lines) ==="
        tail -100 specs-reporter.log
    
        # ─────────────────────────────────────────────────────────────────────────
    # EXPORT FAILED TEST CASES TO GOOGLE SHEETS
    # Requires secrets:
    #   GSHEET_SERVICE_ACCOUNT  – JSON key of a Service Account that has
    #                             Editor access to the target spreadsheet
    #   GSHEET_SPREADSHEET_ID   – ID from the Google Sheets URL
    # ─────────────────────────────────────────────────────────────────────────
    - name: Export Failed Test Cases to Google Sheets
      if: ${{ !cancelled() }}
      run: |
        pip install --quiet google-auth google-auth-httplib2 google-api-python-client

        python3 << 'PYEOF'
        import os, re, json, datetime
        from google.oauth2 import service_account
        from googleapiclient.discovery import build

        # ── Config ────────────────────────────────────────────────────────────
        SA_JSON      = os.environ["GSHEET_SERVICE_ACCOUNT"]
        SPREADSHEET  = os.environ["GSHEET_SPREADSHEET_ID"]
        LOG_PATH     = "specs-reporter.log"

        SCOPES = ["https://www.googleapis.com/auth/spreadsheets"]

        # ── Auth ──────────────────────────────────────────────────────────────
        creds   = service_account.Credentials.from_service_account_info(
                      json.loads(SA_JSON), scopes=SCOPES)
        service = build("sheets", "v4", credentials=creds)
        sheets  = service.spreadsheets()

        # ── Parse failed test cases from log ─────────────────────────────────
        # WDIO spec reporter marks a failed spec with lines like:
        #   [chrome] FAILED in Chrome - /path/to/feature/INT51-01_something.feature
        # then the describe / it hierarchy follows.  We collect:
        #   • the spec-file basename  → used as test-case ID  (column A)
        #   • "Fail"                  → Status column          (column D)
        #   • "Automation"            → Type column            (column C)
        # ─────────────────────────────────────────────────────────────────────
        failed_cases = []

        # Pattern 1 – lines that contain the word FAILED followed by a file path
        # e.g.  "FAILED in Chrome - /home/runner/work/.../INT51-01_editWebhook.feature"
        spec_fail_re = re.compile(
            r'FAILED.*?([A-Z]{2,}[\w-]*\d[\w-]*)[_\s]',   # capture ID like INT51-01
            re.IGNORECASE
        )

        # Pattern 2 – cucumber/mocha "✖" or "× failing" blocks
        # e.g.  "  1) INT51-01: Edit Webhook modal …"
        numbered_fail_re = re.compile(
            r'^\s*\d+\)\s+([\w-]+(?:-\d+)+[^—\n]*)',      # "1) INT51-01: …"
        )

        # Pattern 3 – lines explicitly marked as "failed" after test name
        # e.g.  "INT51-01: Edit Webhook … – failed"
        inline_fail_re = re.compile(
            r'([\w-]+-\d{2}[^:\n]*).*?(?:failed|FAILED)',
            re.IGNORECASE
        )

        seen = set()

        if os.path.exists(LOG_PATH):
            with open(LOG_PATH, encoding="utf-8", errors="replace") as fh:
                for line in fh:
                    for pattern in (spec_fail_re, numbered_fail_re, inline_fail_re):
                        m = pattern.search(line)
                        if m:
                            raw = m.group(1).strip()
                            # Normalise to "INTXX-YY: rest of title" if possible
                            id_m = re.match(r'([A-Z]{2,}\d*-\d+)(.*)', raw, re.IGNORECASE)
                            if id_m:
                                tc_id   = id_m.group(1).upper()
                                tc_desc = id_m.group(2).strip(" :-")
                                label   = f"{tc_id}: {tc_desc}" if tc_desc else tc_id
                            else:
                                label = raw

                            if label not in seen:
                                seen.add(label)
                                failed_cases.append(label)
                            break

        if not failed_cases:
            print("No failed test cases found — skipping Google Sheets export.")
            raise SystemExit(0)

        print(f"Found {len(failed_cases)} failed test case(s):")
        for tc in failed_cases:
            print(f"  • {tc}")

        # ── Resolve target sheet name (MM/DD, or MM/DD-2, MM/DD-3 …) ────────
        tz        = datetime.timezone(datetime.timedelta(hours=7))
        today     = datetime.datetime.now(tz)
        base_name = today.strftime("%m/%d")          # e.g. "05/21"

        meta      = sheets.get(spreadsheetId=SPREADSHEET).execute()
        existing  = {s["properties"]["title"] for s in meta["sheets"]}

        sheet_name = base_name
        idx        = 2
        while sheet_name in existing:
            sheet_name = f"{base_name}-{idx}"
            idx += 1

        # ── Create the new sheet ─────────────────────────────────────────────
        add_req = {
            "addSheet": {
                "properties": {
                    "title": sheet_name,
                    "gridProperties": {"rowCount": max(200, len(failed_cases) + 10),
                                       "columnCount": 5}
                }
            }
        }
        resp       = sheets.batchUpdate(spreadsheetId=SPREADSHEET,
                                        body={"requests": [add_req]}).execute()
        new_sheet_id = resp["replies"][0]["addSheet"]["properties"]["sheetId"]
        print(f"Created sheet: '{sheet_name}' (id={new_sheet_id})")

        # ── Write header row (row 1) ─────────────────────────────────────────
        # Columns: A=Test Case Name, B=Run, C=Type, D=Status, E=Note
        header = [["Test Case Name", "Run", "Type", "Status", "Note"]]
        sheets.values().update(
            spreadsheetId=SPREADSHEET,
            range=f"'{sheet_name}'!A1:E1",
            valueInputOption="RAW",
            body={"values": header}
        ).execute()

        # ── Write failed test case rows ───────────────────────────────────────
        rows = []
        for tc in failed_cases:
            rows.append([tc, "TRUE", "Automation", "Fail", ""])

        if rows:
            sheets.values().update(
                spreadsheetId=SPREADSHEET,
                range=f"'{sheet_name}'!A2:E{1 + len(rows)}",
                valueInputOption="RAW",
                body={"values": rows}
            ).execute()

        # ── Apply formatting to match the reference screenshot ───────────────
        #    • Header row: bold, dark background, white text
        #    • "Fail" cells in column D: red background, white text
        #    • "Pass" would be green – not needed here (only failed cases)
        #    • Column A wider, columns B-E auto

        RED_BG    = {"red": 0.863, "green": 0.196, "blue": 0.184}   # #DC3220
        WHITE_TXT = {"red": 1,     "green": 1,     "blue": 1}
        HEADER_BG = {"red": 0.2,   "green": 0.2,   "blue": 0.2}

        def cell_range(sheet_id, r1, c1, r2, c2):
            return {"sheetId": sheet_id,
                    "startRowIndex": r1, "endRowIndex": r2,
                    "startColumnIndex": c1, "endColumnIndex": c2}

        format_requests = [
            # Bold header
            {
                "repeatCell": {
                    "range": cell_range(new_sheet_id, 0, 0, 1, 5),
                    "cell": {
                        "userEnteredFormat": {
                            "backgroundColor": HEADER_BG,
                            "textFormat": {"bold": True,
                                           "foregroundColor": WHITE_TXT,
                                           "fontSize": 10},
                            "horizontalAlignment": "CENTER",
                            "verticalAlignment": "MIDDLE"
                        }
                    },
                    "fields": "userEnteredFormat(backgroundColor,textFormat,"
                              "horizontalAlignment,verticalAlignment)"
                }
            },
            # Red "Fail" badge – column D (index 3), data rows
            {
                "repeatCell": {
                    "range": cell_range(new_sheet_id, 1, 3, 1 + len(rows), 4),
                    "cell": {
                        "userEnteredFormat": {
                            "backgroundColor": RED_BG,
                            "textFormat": {"bold": True,
                                           "foregroundColor": WHITE_TXT},
                            "horizontalAlignment": "CENTER"
                        }
                    },
                    "fields": "userEnteredFormat(backgroundColor,textFormat,"
                              "horizontalAlignment)"
                }
            },
            # Auto-resize all columns
            {
                "autoResizeDimensions": {
                    "dimensions": {
                        "sheetId": new_sheet_id,
                        "dimension": "COLUMNS",
                        "startIndex": 0,
                        "endIndex": 5
                    }
                }
            },
            # Freeze header row
            {
                "updateSheetProperties": {
                    "properties": {
                        "sheetId": new_sheet_id,
                        "gridProperties": {"frozenRowCount": 1}
                    },
                    "fields": "gridProperties.frozenRowCount"
                }
            }
        ]

        sheets.batchUpdate(spreadsheetId=SPREADSHEET,
                           body={"requests": format_requests}).execute()

        print(f"✅ Exported {len(rows)} failed test case(s) to sheet '{sheet_name}'.")
        PYEOF
      env:
        GSHEET_SERVICE_ACCOUNT: ${{ secrets.L_GSHEET_SERVICE_ACCOUNT }}
        GSHEET_SPREADSHEET_ID:  ${{ secrets.L_GSHEET_SPREADSHEET_ID }}

    - name: Notify Google Chat
      if: ${{ !cancelled() }}
      run: |
        STATUS="PASSED"
        STATUS_COLOR="#0F9D58"
        STATUS_ICON="✅"

        if [ "${{ job.status }}" != "success" ]; then
          STATUS="FAILED"
          STATUS_COLOR="#DB4437"
          STATUS_ICON="❌"
        fi

        export STATUS STATUS_COLOR STATUS_ICON
        export DATE_TIME="$(date '+%d/%m/%Y %H:%M:%S')"
        export REPORT_URL="https://record360-report-s.web.app"
        export BRANCH_WDIO="${{ github.event.inputs.branchWdio || 'master' }}"
        export BRANCH_DASHBOARD="${{ github.event.inputs.branchDashboard || 'develop' }}"

        envsubst < .github/googleChat/dashboard-card.json > payload.json

        curl -X POST "${{ secrets.GCHAT_WEBHOOK }}" \
          -H "Content-Type: application/json" \
          -d @payload.json